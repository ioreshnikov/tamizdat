version: "3.8"

services:
  bot:
    env_file: "tamizdat.env"
    build:
      context: .
      dockerfile: "Dockerfile"
    volumes:
      - ./data:/tamizdat/data
    network_mode: "host"
    restart: unless-stopped
